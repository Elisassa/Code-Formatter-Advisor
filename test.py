# A simple Python script with some formatting issues
def greet(name):
    print(f"Hello, {name}!")  # This line is properly formatted

# This function has inconsistent indentation and style issues
def add_numbers(a, b) :
    result = a+b
     return result

greet("World")

# Calling the add_numbers function with some test values
print(add_numbers(5,10))
